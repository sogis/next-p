allprojects {
    layout.buildDirectory.set(
        file("${rootProject.projectDir}/netl_build/${project.name}")
    )
}

tasks.register('copy', Copy) {
    from layout.projectDirectory.dir("repo/themes/afu/kbs")
    into layout.buildDirectory
}